class Solution {
public:
    vector<int> countSmaller(vector<int>& nums) {
        //vector<int> res(nums.size(), 0);
		
		vector<int> bit(nums.size() + 1, 0);
	
	}

private:
	int lowbit(int idx) {
		
	}
		
		
};